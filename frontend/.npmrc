# ============================================================
# npm 项目级配置（放在 frontend/，npm 从 CWD 读取）
#
# registry：registry.npmjs.org 在本机**不可达**（TLS 握手超时），
#           必须走 npmmirror，否则任何 install 都会挂住。
# ============================================================
registry=https://registry.npmmirror.com/
fund=false
audit=false

# ============================================================
# ⚠️ 关于 cache：不能写在这里
#
# npm 默认缓存目录为 C:\Users\<user>\AppData\Local\npm-cache，在本机受限
# 环境下会直接报 EPERM（拒绝访问），必须重定向到项目内 .cache/npm。
#
# 但本项目的路径**含空格**（".../Flower Recognition System/..."），
# 写成 `cache=D:/.../Flower Recognition System/.cache/npm` 时 npm 的 ini
# 解析会把空格后的部分截断，导致设置**静默失效**（已实测：
# `npm config get cache` 仍返回 C 盘默认值）。
#
# 因此改为用**环境变量**设置（优先级更高，且不受 ini 解析影响）：
#     $env:npm_config_cache = "<项目根>\.cache\npm"
#
# 已固化在 scripts\frontend.ps1 里，日常请用该脚本执行 npm 命令。
# ============================================================
